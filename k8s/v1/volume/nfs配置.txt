1.master和work node节点执行
    yum install -y nfs-utils rpcbind

2.master 节点修改配置
    vi /etc/exports 加入配置
    /nfsdata   *(rw,sync,no_root_squash)

3. master 节点系统自动启动
    systemctl enable --now rpcbind
    systemctl enable --now nfs
